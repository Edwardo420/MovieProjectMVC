@model Movie
@using MovieStructure;

<div class="container marginTop">
    <div class="row">
        <div class="card">
            <div class="text-center view overlay hm-white-slight">
                <img src="@Model.Image" class="img-fluid" alt="">
                <h4 class="card-title" style="text-align: center">Title: @Model.Title</h4>
                <a href="#">
                    <div class="mask waves-effect waves-light"></div>
                </a>
            </div>
            <div class="card-block" style="float:right; display: inline-block">
                <div class="addToCart text-right">
                    <p class="button ">
                        <a class="btn btn-primary" id="cartButton" asp-controller="FavoriteList" asp-action="AddToFavoriteList" asp-route-movieId="@Model.MovieID">
                            Add @Model.Title to Favorite List
                        </a>
                    </p>
                </div>
                <h4 class="card-text">Information about @Model.Title : @Model.Description</h4>
            </div>
            <br/>
            <img src="@Model.Image" class="card-img-bottom" alt="">
        </div>
    </div>
</div>